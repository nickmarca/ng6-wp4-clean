import { Component } from '@angular/core';

@Component({
    selector: 'my-app',
    template: `
      <div>
        <h1>HELLO WORLD !</h1>
        
        <p>This project is a clean Angular starter template who I plan to maintain as far as I use Angular at my Job. </p>
        <p>The reason why I decided to create this is because I want to have a clean, not opinated and light alternative to Angular Cli. </p>
        <p>This is not suppose to do all the things Angular CLI does but it is suppose to do the *main* job Angular CLI does that is transpile Angular Typescript code.</p>
      </div>
    `
})
export class AppComponent { }